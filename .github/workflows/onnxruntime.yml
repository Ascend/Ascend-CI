name: onnx runtime build and test

on:
  push:
    branches:
      - main
      - onnx
    paths:
      - '.github/workflows/onnxruntime.yml'
  pull_request:
    branches:
      - main
      - onnx
    paths:
      - '.github/workflows/onnxruntime.yml'
  schedule:
    - cron: '0 12 * * *'
  issue_comment:
    types: [created]

jobs:
  build-and-test:
    name: Build and test onnx runtime from source
    if: ${{ github.repository_owner == 'Ascend' && (contains(github.event.comment.body, 'recheck') || github.event_name == 'schedule' || github.event_name == 'push') }}
    runs-on: linux-arm64-npu-1
    container:
      image: swr.cn-southwest-2.myhuaweicloud.com/base_image/ascend-ci/cann:latest
      options: --entrypoint /bin/sh

    steps:
      - name: Show NPU info
        run: |
          npu-smi info

      - name: Config mirrors
        run: |
          sed -i 's|ports.ubuntu.com|mirrors.tuna.tsinghua.edu.cn|g' /etc/apt/sources.list
          pip config set global.index-url https://mirrors.tuna.tsinghua.edu.cn/pypi/web/simple

      - name: Install system dependencies
        run: |
          apt-get update
          apt-get install -y \
              gcc g++ ninja-build libssl-dev libffi-dev libbz2-dev zlib1g-dev libreadline-dev libsqlite3-dev

          pip install --upgrade pip
          pip install make cmake twine \
            packaging \
            wheel \
            numpy==1.24.0 \
            decorator \
            attrs \
            flatbuffers \
            protobuf==3.20.3

      - name: Checkout onnx
        uses: actions/checkout@v4
        with:
          repository: microsoft/onnxruntime
          path: onnxruntime

      - name: Build and test onnx runtime
        working-directory: onnxruntime
        run: |
          ./build.sh --allow_running_as_root --config RelWithDebInfo --build_shared_lib --parallel --use_cann --build_wheel --skip_tests

    # steps:
    #   - name: Install Docker
    #     run: |
    #       # 下载并执行Docker官方安装脚本
    #       curl -fsSL https://get.docker.com -o get-docker.sh
    #       sudo sh get-docker.sh

    #       # 启动Docker服务
    #       sudo systemctl start docker
    #       sudo systemctl enable docker

      
      # - name: Start Docker service (robust method with fallbacks)
      #   run: |
      #     sudo nohup dockerd > /dev/null 2>&1 &
      #     if [ $? -eq 0 ]; then
      #       echo "Docker daemon started directly with dockerd"
      #       # Wait for daemon to initialize
      #       sleep 5
      #       exit 0
      #     else
      #       echo "ERROR: All methods failed to start Docker"
      #       exit 1
      #     fi

      # - name: Verify Docker service is running
      #   run: |
      #     # Wait for Docker daemon to initialize
      #     for i in {1..10}; do
      #       if sudo docker info > /dev/null 2>&1; then
      #         echo "Docker daemon is running"
      #         exit 0
      #       fi
      #       echo "Waiting for Docker daemon to start... ($i/10)"
      #       sleep 2
      #     done
      #     echo "Docker daemon failed to start after 20 seconds"
      #     exit 1

      # - name: Checkout onnx
      #   uses: actions/checkout@v4
      #   with:
      #     repository: microsoft/onnxruntime
      #     path: onnxruntime

      # - name: Build and test onnxruntime in container
      #   run: |
      #     sudo docker run --rm \
      #       --user root \
      #       -v "$PWD:/workspace" \
      #       -w /workspace/onnxruntime \
      #       swr.cn-north-4.myhuaweicloud.com/bachelor/manylinux_2_28-py311-cann8_2-aarch64:latest \
      #       bash -lc '
      #         set -euxo pipefail

      #         echo "=== NPU info ==="
      #         npu-smi info || true

      #         echo "=== Source Ascend env ==="
      #         source /usr/local/Ascend/ascend-toolkit/set_env.sh

      #         echo "ASCEND_TOOLKIT_HOME=$ASCEND_TOOLKIT_HOME"

      #         echo "=== Build onnxruntime ==="
      #         ./build.sh \
      #           --allow_running_as_root \
      #           --config RelWithDebInfo \
      #           --build_shared_lib \
      #           --parallel \
      #           --use_cann \
      #           --build_wheel \
      #           --skip_tests
      #       '